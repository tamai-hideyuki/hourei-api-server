version: 1.0
runtime: php81

env:
  variables:
    APP_ENV: production
    APP_DEBUG: "false"
    APP_KEY: base64:0Ab/FWxmAC4Axspzm3viH9S5hVYJxfNoL/nunklt/4k=

build:
  commands:
    - composer install --no-dev --optimize-autoloader

run:
  command: php artisan serve --host=0.0.0.0 --port=8080

network:
  port: 8080
