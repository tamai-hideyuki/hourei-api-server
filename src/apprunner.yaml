version: 1.0
runtime: php81
runtime-version: 8.1.22
env:
  variables:
    APP_ENV: production
    APP_DEBUG: "false"

build:
  commands:
    build:
      - composer install --no-dev --optimize-autoloader

run:
  command: php artisan serve --host=0.0.0.0 --port=8080
  network:
    port: 8080
